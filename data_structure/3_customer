CREATE OR REPLACE TABLE CUSTOMERS (
  CUSTOMER_ID   STRING,
  EMAIL         STRING,
  SIGNUP_DATE   TIMESTAMP_NTZ,
  COUNTRY       STRING
);

COPY INTO CUSTOMERS
FROM @STG_RAW_FINANCE/customers.csv
ON_ERROR = 'CONTINUE';
